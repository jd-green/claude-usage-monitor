# No secrets required — auth comes from the local Claude Code login
# (macOS Keychain / ~/.claude/.credentials.json).

# Optional: default poll interval in seconds (min 30)
CLAUDE_MONITOR_INTERVAL=60
